apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:9090   # service DNS within docker-compose network
    isDefault: true
    editable: true
